{
  "name": "timer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify index.js > bundle.js && uglifyjs --compress --mangle bundle.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^16.5.1"
  },
  "dependencies": {
    "easytimer.js": "^4.1.1",
    "jquery": "^3.5.1"
  }
}
